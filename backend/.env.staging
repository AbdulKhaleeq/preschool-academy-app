# STAGING Environment Configuration
NODE_ENV=staging

# Database Configuration (Railway will provide DATABASE_URL)
# These fallbacks are for Railway's PostgreSQL service
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=
# Railway will inject DATABASE_URL automatically

# JWT Configuration - STAGING SECRET (Different from production)
JWT_SECRET=927bdb2f0e7ed044321d1701db221ff0b08360371b30c1ef05aefc675ecb751d

# Admin Configuration - Use test phone numbers for staging
ADMIN_PHONES=9876543210,1234567890

# Server Configuration
PORT=4000

# Redis Configuration (Railway will provide REDIS_URL)
# REDIS_URL will be auto-injected by Railway Redis service

# CORS Origins (Staging frontend URL - will be updated after Vercel deployment)
ALLOWED_ORIGINS=https://preschool-academy-staging.vercel.app,https://your-staging-frontend.vercel.app

# SSL Configuration
FORCE_HTTPS=true

# Email Configuration (Optional for staging)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# SMS Configuration (Optional for staging)
SMS_API_KEY=
SMS_SENDER_ID=PRESCHOOL

# Staging Specific Settings
STAGING_MODE=true
LOG_LEVEL=debug